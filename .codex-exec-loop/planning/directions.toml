version = 1

[queue_idle]
policy = "review_and_enqueue"
prompt_path = ".codex-exec-loop/planning/prompts/queue-idle-review.md"

[[directions]]
id = "context-first-architecture-and-doc-coherence"
title = "Context-first architecture and doc coherence"
summary = "Keep the residual context-first cleanup parked behind the shipped architecture audit and shell-boundary work so only directly justified operator-copy follow-ups remain."
success_criteria = [
    "The shipped hotspot order and capability-boundary notes stay stable enough that follow-up slices do not need to reopen the original audit.",
    "Operator-facing wording follow-ups remain narrow and tied to one clear benefit instead of restarting a broad vocabulary rewrite.",
    "Any remaining context-first task can point to one bounded code cluster without re-expanding docs/plan into backlog history.",
]
scope_hints = [
    "Treat this as a parked follow-up lane unless a concrete shell-copy or operator wording issue proves it should re-enter the queue.",
    "Prefer bounded wording or projection slices over reopening the completed context-first roadmap wholesale.",
    "Keep docs/design as shipped truth and use docs/plan only for new reviewable follow-up slices.",
]
detail_doc_path = ".codex-exec-loop/planning/directions/context-first-architecture-and-doc-coherence.md"
state = "paused"

[[directions]]
id = "terminal-agent-bridge-research-and-capability-boundary"
title = "Terminal agent bridge research and capability boundary"
summary = "Preserve `docs/plan/21` through `26` as the capability and transport baseline for future non-Codex work without treating any terminal bridge transport as the active product path."
success_criteria = [
    "The bridge hub, transport matrix, capability boundary note, and experiment matrix remain a stable reference set for future non-Codex exploration.",
    "The research docs keep candidate transport families explicit without presenting tmux or any other bridge transport as the current shipped runtime path.",
    "Capability-first seams remain the contract so future headless runner work does not reopen a fake universal provider API.",
]
scope_hints = [
    "Treat this direction as a completed research baseline rather than the place to queue new implementation tasks.",
    "Use it when future work needs transport comparison or capability vocabulary, not when choosing the active product runtime.",
    "Keep the research docs concise and current after the tmux-specific readiness package is removed from the official set.",
]
detail_doc_path = ".codex-exec-loop/planning/directions/terminal-agent-bridge-research-and-capability-boundary.md"
state = "done"

[[directions]]
id = "claude-first-headless-cli-runner"
title = "Claude-first headless CLI runner"
summary = "Keep the main interactive runtime on Codex app-server while preparing a Claude-first headless runner for `PlanningWorkerPort` and sub-task flows only."
success_criteria = [
    "The shell bootstrap path stays directly wired to `CodexAppServerAdapter` with no terminal-bridge selector or tmux-specific environment contract in the product runtime.",
    "The next executable work fixes `PlanningWorkerPort` as the extension seam for a Claude-first headless runner while leaving `AppServerPlanningWorkerAdapter` as the current shipped implementation.",
    "Main interactive Claude, SSH or tunnel mediation, proxy mediation, and any managed wrapper details beyond planning-worker scope stay explicitly out of scope.",
]
scope_hints = [
    "Keep concrete execution slices in task-ledger.json and use this direction plus its detail doc for strategy, sequencing, and scope control.",
    "Treat the current product baseline as `Codex main interactive + Claude hidden planning or sub-task runner`, not as a new general terminal bridge rollout.",
    "Use attachment vocabulary generically, but defer the final headless attachment-profile constructor choice until the first bounded runner design slice.",
]
detail_doc_path = ".codex-exec-loop/planning/directions/claude-first-headless-cli-runner.md"
state = "active"
